/// User Main View
///
/// Shows the welcome screen with map illustration before user accesses the interactive map.

import SwiftUI

struct UserMainView: View {
    var body: some View {
        WelcomeMapScreen()
    }
}

#Preview {
    UserMainView()
}
